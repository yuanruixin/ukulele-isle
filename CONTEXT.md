# CONTEXT.md — 尤克里里学习网站

领域术语与共享语言。只记录领域术语，不记录实现细节。

## 曲谱 (Score)

- **Score（曲谱）**：一首歌曲的可视化乐谱与可演奏数据，是学习的核心对象。
- **alphaTex**：alphaTab 的原生文本谱面 DSL，是本项目中所有 **Score** 的唯一存储格式。
- **Ukulele Tuning（尤克里里调弦）**：GCEA 四弦调弦，**Score** 默认使用此调弦。

## 和弦 (Chord)

- **Chord Library（和弦库）**：常用和弦指法的速查工具，也是「点一下听声音」的试听入口。第一版收录 C / F / Am / G 四个（C 调的 I–IV–vi–V），数据在 `siteConfig.chords.items`，数组顺序即页面顺序。
- **Chord Diagram（指法图）**：和弦在指板上的按法图。竖线是弦、横线是品，顶部粗线是**琴枕**，弦顶的 **○ = 空弦**、**× = 不弹**，圆点里的数字是**手指编号**（1 食指 / 2 中指 / 3 无名指 / 4 小指）。
  - ⚠️ **横向方向**：最左是 **4 弦（G）**、最右是 **1 弦（A）**，这是尤克里里和弦表的通行习惯（和吉他谱一致）。而数据里的 `frets` 顺序与 alphaTab 一致是**高音弦在前**（1→4 弦），所以画图时横向要反过来算。
- **Finger（手指编号）**：写指法时用哪根手指按，只影响图上圆点里的数字，不影响发声。
- **Strum（下扫）**：试听的发声方式——四根弦从 **4 弦依次扫到 1 弦**，而不是同时响。alphaTex 里用 beat 级的 `{bd N}`（BrushDown）表达，`N` 是刷弦时长（tick）；实测 alphaTab 的 Down 变体正是 4弦→1弦，与物理下扫一致。
- **Strum Spread（扫弦跨度）**：第一根弦到最后一根弦的时间差（`strumSpreadMs`，默认 160ms）。图上的圆点也是按这个间隔依次点亮的——**视觉与发声用同一组换算**（`strumTiming`），所以不会各走各的。
- **Ring（余音）**：扫完之后和弦能响多久（`ringSeconds`，默认 2.4s）。实现上是把和弦写成一个**全音符**撑满一小节，再由它反推试听速度。
- **单一数据源**：指法图与试听谱都从 `siteConfig.chords.items` 派生（`src/lib/chord.ts`），因此「图上画的」和「耳朵听到的」永远一致。
- **静默预加载**：进和弦页后，在浏览器空闲时（`requestIdleCallback`）后台取 alphaTab 与音色库，这样用户点下去就已经能响，页面本身又不用背这 4.3MB。

## 调音 (Tuning)

- **Tuner（调音器）**：听音校准工具，用设备麦克风检测琴弦基频，与目标频率比对给出偏差。
- **Standard Tuning（标准调弦）**：GCEA——4 弦 G4 (392.0 Hz)、3 弦 C4 (261.63 Hz)、2 弦 E4 (329.63 Hz)、1 弦 A4 (440.0 Hz)。
- **Target String（目标弦）**：当前待校准的弦。有两种确定方式，**二者互斥**——
  - **手动模式**：点哪根弦校哪根，**默认不预选**（按钮均不高亮）；再点一次已选中的弦可取消。
  - **Auto Mode（自动模式）**：开关打开后不用先选弦，弹哪根自动认哪根。
- **Auto Mode（自动识别）**：从 **Target String** 候选里挑与实测频率最接近的一根；连续若干帧都指向同一根才真正切弦（防抖），锁定前不点亮任何弦。锁定后用**呼吸光环**标记（与手动选中的实心按钮区分）。听清的音**明显不属于任何一根弦**时连续若干帧后释放锁定，退回未选弦态。
- **Match Window（识别窗口）**：实测频率与候选弦相差在 ±100 音分内才算「像是这根弦」。相邻弦相距 200 音分，±100 正好铺满且不重叠；超出窗口说明这根弦跑得太远，或用的是别的音，此时退化为**未选弦态**的十二平均律读数。
- **未选弦态**：还没点选目标弦、或自动模式尚未锁定时——读数显示**实测音名**（如 C4）与该音相对最近十二平均律音高的偏差，保证一拨弦就有反馈；文案用「音偏高 / 音偏低」。
- **Cents（音分）**：音高偏差单位，1 个半音 = 100 音分。指示盘为**横向**刻度，范围 ±50 音分，指针圆环左右移动（偏左 = 音偏低）。
- **In Tune（准了）**：|偏差| ≤ 容差（默认 ±5 音分），音名、指针与弦钮同步转为绿色。
- **Tuned / 已校准（逐弦累积）**：**Target String** 一旦被判定准了，它的弦钮就**保持绿色**——琴弦余音衰减、改选别的弦都不会让绿色消失，这样一眼能看出 CGEA 四根里哪些调好了。之后再弹同一根弦明显跑调时会熄灭；停止聆听则整轮清零（重新开始一轮校准）。
  - ⚠️ 若把绿色设计成「当前正在响且准了」的瞬时状态，余音一停绿点就消失，用户没法累积判断进度。
- **调高 / 调低**：已选弦时的动作提示——音偏低提示「调高」（拧紧弦），音偏高提示「调低」。
- **自动开麦**：点选 **Target String** 或打开 **Auto Mode** 时自动申请并开启麦克风，省掉「先点弦、再点开启麦克风」两步；**取消选择**（再点一次已选中的弦）不触发。麦克风按钮因此主要承担「停止」职责。
- **Signal Diagnostics（信号诊断）**：把「输入设备 → 波形 → 基频检测 → 判定」每一步摊开显示的面板（设备下拉、权限、RMS/峰值、基频与置信度、逐帧判定、滚动日志）。**默认不渲染**，仅在调试模式下出现——把 `siteConfig.tuner.debug` 改成 true，或在地址栏用 `/#/tools/tuner?debug=1` 临时打开。日常界面只保留**虚拟音频设备**这一条告警（它会直接导致「拨弦毫无反应」）。
- **Headstock Asset（琴头素材）**：从设计参考截图抠出的透明 PNG（琴头 + 琴颈 + 指板），作为调音器页的视觉主体与调弦按钮的定位基准；素材本身不含任何高亮状态。

## 歌曲 (Song)

- **Song（歌曲）**：可学习的一首曲子，包含元数据（标题、艺术家、标签）与一份 **Score**。
- **搜索**：曲谱库页对 **Song** 按标题、艺术家、标签做本地实时过滤。
- **Song Folder（歌曲文件夹）**：一首 **Song** 在仓库中的存储单元，包含 meta.json 与 score.tex。
- **Song Index（歌曲索引）**：构建时扫描所有 **Song Folder** 生成的歌曲清单，供列表页与搜索使用。
- **Site Config（站点配置）**：项目顶层的配置文件，存放站点级可调项（主题、播放默认设置等）。

## 平台

- **纯静态站**：整个网站无后端，构建产物可直接部署到任意静态托管。
- **Apple HIG 风格**：UI 遵循苹果设计语言——毛玻璃、大圆角、大留白、细腻动效。
- **Appearance（外观）**：站点的亮 / 暗配色。首次进入跟随系统外观，之后以用户**手动切换**为准并记住该选择。
- **Appearance Switch（外观开关）**：切换 **Appearance** 的开关——一个浅色轨道加一个浮起的圆形滑块，滑块里只显示**当前外观**的那一枚图标（暗色月亮 / 亮色太阳），切换到哪一边就显示哪一枚。

## 演奏 (Playback)

- **Playback（演奏）**：由 alphaTab 内置 SoundFont 播放器驱动，音源即 **Score** 本身，不使用原曲音频。
- **Beat Highlight（节拍高亮）**：演奏时高亮当前正在发声的节拍，与 **Playback** 天然同步。
- **Mute（静音）**：播放控制条提供一键静音（masterVolume 0/1 切换）。

## 页面

- **首页**：叙事区（站点名 + 副标题）+「曲谱库 / 工具」入口卡片。
- **曲谱库页**：搜索框 + 歌曲卡片列表，消费 **Song Index**。
- **工具页**：练琴工具入口列表——已实现的工具可点击进入，规划中的置灰并带「敬请期待」角标。
- **和弦库页**：**Chord Library** 的完整界面——网格卡片（移动端 2×2 / PC 一行四个），卡片整体可点即试听，选中的那张用 accent 描边并点亮指法图。页面上不出现谱面，alphaTab 只作为屏幕外的合成器使用。
- **调音器页**：**Tuner** 的完整界面——音分刻度指示盘 + 琴头素材 + 四根弦选择按钮 + **Auto Mode** 开关 + 麦克风开关。**移动端**单栏竖排（≤768px）；**PC**（≥1024px）两栏——左栏「指示盘 + 麦克风开关」，右栏「琴头与选弦」。整页在一个屏幕内不滚动。
- **歌曲详情页**：谱面渲染 + 播放控制条 + **Beat Highlight**，承载学习闭环。
- **导航栏**：毛玻璃吸顶；桌面端右侧平铺「曲谱 / 工具」入口（当前页高亮）+ **Appearance Switch**，移动端（<640px）收进右上角汉堡按钮——点击后三条横线变为叉号，菜单为纯色全窗口浮层（fixed inset-0 铺满窗口，导航栏在更高层保留叉号，锁定背景滚动），选项从导航栏下方开始从上到下依次展开，最下方是独立的「外观」区块（卡片形式，与上方导航入口区分开），路由变化自动收起。
